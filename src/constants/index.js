import { EMPLOYEE_ID_SESSION_KEY } from './sessionStorageKeys'
import { ORDER_BY } from './localStorageKeys'
import { EMPLOYEE_COLLECTION, CHALLENGES_COLLECTION } from './firestoreCollection'

export {
  // session storage keys
  EMPLOYEE_ID_SESSION_KEY,

  // local storage keys
  ORDER_BY,

  // firestore collection names
  EMPLOYEE_COLLECTION,
  CHALLENGES_COLLECTION
}